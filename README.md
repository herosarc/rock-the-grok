# rock-the-grok

Rock the Grok lead magnet landing (Hero's Arc).

Live: https://herosarc.github.io/rock-the-grok/

Static site deployed to GitHub Pages by the `deploy-pages.yml` workflow: every push to `main` publishes the repo root. No build step, the files in the repo are the site.
