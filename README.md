# rock-the-grok

Rock the Grok lead magnet landing (Hero's Arc).

Live: https://herosarc.github.io/rock-the-grok/

Static site served by GitHub Pages from the `gh-pages` branch (same pattern as TimeKeep). `main` holds the source of truth; to deploy, update `main` and fast-forward `gh-pages` to it.
